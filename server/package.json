{
  "name": "nursing-home-server",
  "version": "1.0.0",
  "description": "Backend server for Nursing Home Management System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "reset-db": "rm -f nursing_home.db && node -e \"const fs = require('fs'); if(fs.existsSync('nursing_home.db')) fs.unlinkSync('nursing_home.db'); console.log('Database reset');\""
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^2.0.2",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
